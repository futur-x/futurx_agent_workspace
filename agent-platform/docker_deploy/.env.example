# FuturX Agent Workspace - Production Environment Configuration
# 复制此文件为 .env 并修改配置

# ========== 必须修改的配置 ==========
# JWT密钥 - 请使用强密码，可以用命令生成: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# ========== 可选配置 ==========
# 会话过期时间 (默认30分钟)
SESSION_EXPIRY=30m

# 最大文件上传大小 (字节，默认10MB)
MAX_FILE_SIZE=10485760

# CORS允许的前端地址
# 如果前端和后端在同一服务器，使用 http://localhost:5173
# 如果使用域名访问，改为您的域名，如 https://yourdomain.com
CORS_ORIGIN=http://localhost:5173

# ========== 管理员账号配置 ==========
# 首次启动时自动创建的管理员账号
# 请在生产环境中修改这些默认值
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# 数据库配置（如果使用外部数据库）
# DATABASE_URL=postgresql://user:password@localhost:5432/faw_db

# ========== Dify API 配置（如需要） ==========
# DIFY_API_URL=https://your-dify-instance.com/api
# DIFY_API_KEY=your-dify-api-key